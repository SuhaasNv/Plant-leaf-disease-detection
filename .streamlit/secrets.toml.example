# Copy to secrets.toml and set your Google Drive shareable link.
# Do not commit secrets.toml (it is in .gitignore).
#
# 1. Upload trained_plant_disease_model.h5 to Google Drive.
# 2. Right-click file → Share → set to "Anyone with the link" (Viewer).
# 3. Copy the link and paste below (one of these formats):
#
# Share link:  https://drive.google.com/file/d/YOUR_FILE_ID/view?usp=sharing
# Direct link: https://drive.google.com/uc?id=YOUR_FILE_ID
#
# For large files (>100MB), the direct link often works better.

MODEL_URL = ""
